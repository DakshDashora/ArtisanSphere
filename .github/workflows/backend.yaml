name: Deploy FastAPI Backend

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
<<<<<<< HEAD
      # Checkout repo
      - uses: actions/checkout@v3

      # Authenticate with GCP
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # Configure gcloud CLI
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          export_default_credentials: true

      # Build & Push Docker image
      - name: Build and Push Docker image
        run: |
          gcloud builds submit --tag gcr.io/${{ secrets.GCP_PROJECT_ID }}/my-backend:latest .

      # Deploy to Cloud Run
      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v1
        with:
          service: my_backend
          image: gcr.io/${{ secrets.GCP_PROJECT_ID }}/my_backend:latest
          region: us-central1
=======
      # Checkout the repo
      - uses: actions/checkout@v3

      # Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # Install dependencies
      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: ./backend

      # Build & deploy Docker image to Cloud Run
      - name: Deploy to Cloud Run
        run: |
          gcloud auth activate-service-account --key-file=$GCP_SA_KEY
          gcloud builds submit --tag gcr.io/$GCP_PROJECT_ID/fastapi-backend ./backend
          gcloud run deploy fastapi-backend --image gcr.io/$GCP_PROJECT_ID/fastapi-backend:latest --region us-central1 --platform managed --allow-unauthenticated
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
>>>>>>> c982d38fbde78e5e640439bf65840f20fc4f969a
